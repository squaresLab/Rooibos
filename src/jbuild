(jbuild_version 1)

(ocamllex
  (lexer))

(menhir 
  ((modules (parser))))


(library
  ((name rooibos_modules)
   (libraries (core_kernel menhirLib))))


(executable
  ((libraries (rooibos_modules))
   (name rooibos)))


(alias
 ((name DEFAULT)
  (deps
   (rooibos.exe))))
